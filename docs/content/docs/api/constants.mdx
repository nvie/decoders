---
title: Constants
description: Decoders for constant and fixed values.
---

## constant

<DecoderSig name="constant" params="value: T" type="T" source="basics.ts#L84-L93" />

Accepts only the given constant value.

<DecoderPlayground
  decoder="constant('hello')"
  examples={["'hello'", "'this breaks'", 'false', 'undefined']}
/>

---

## null\_

<DecoderSig name="null_" type="null" source="basics.ts#L12-L15" />

Accepts and returns only the literal `null` value. <Info>Named `null_` because `null` is a
reserved keyword in TypeScript.</Info>

<DecoderPlayground
  decoder="null_"
  examples={['null', 'false', 'undefined', "'hello world'"]}
/>

---

## undefined\_

<DecoderSig name="undefined_" type="undefined" source="basics.ts#L17-L20" />

Accepts and returns only the literal `undefined` value. <Info>Named `undefined_` because
`undefined` is a reserved keyword in TypeScript.</Info>

<DecoderPlayground
  decoder="undefined_"
  examples={['undefined', 'null', 'false', "'hello world'"]}
/>

---

## enum\_

<DecoderSig
  name="enum_"
  params="enum: MyEnum"
  type="MyEnum"
  source="unions.ts#L99-L120"
/>

Accepts and returns an enum value. <Info>Named `enum_` because `enum` is a reserved
keyword in TypeScript.</Info>

It works with numeric enums:

<DecoderPlayground
  preface={`
    enum Fruit {
      Apple,
      Banana,
      Cherry,
    }
  `}
  globals={{
    Fruit:
      "Object.freeze({ 0: 'Apple', 1: 'Banana', 2: 'Cherry', Apple: 0, Banana: 1, Cherry: 2 })",
  }}
  decoder="enum_(Fruit)"
  examples={['Fruit.Apple', 'Fruit.Banana', '0', '1', '4', "'Apple'"]}
/>

As well as with string enums:

<DecoderPlayground
  globals={{
    Fruit: "Object.freeze({ Apple: 'a', Banana: 'b', Cherry: 'c' })",
  }}
  preface={`
    enum Fruit {
      Apple = 'a',
      Banana = 'b',
      Cherry = 'c',
    }
  `}
  decoder="enum_(Fruit)"
  examples={['Fruit.Apple', 'Fruit.Banana', "'a'", "'b'", "'Apple'", '0']}
/>
