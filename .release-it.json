{
  "$schema": "https://unpkg.com/release-it@21/schema/release-it.json",
  "plugins": {
    "@release-it/keep-a-changelog": {
      "filename": "CHANGELOG.md",
      "addUnreleased": true
    }
  },
  "git": {
    "requireBranch": "main",
    "commitMessage": "Release v${version}",
    "tagName": "v${version}"
  },
  "npm": {
    "skipChecks": true
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
  },
  "hooks": {
    "before:init": ["pnpm run build", "pnpm run lint", "pnpm run lint:package"],
    "after:bump": "prettier --write package.json"
  }
}
